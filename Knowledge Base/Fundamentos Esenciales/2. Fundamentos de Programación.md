# Fundamentos de Programación

### Sintaxis y Semántica

```mermaid
flowchart LR

A["Variables y tipos de datos"]

A --> P
A --> C
A --> S

subgraph P["Primitivos"]
direction TB
P1["Enteros"]
P2["Decimales"]
P3["Booleanos"]
P4["Caracteres"]
P5["Nulo/Vacío"]
end

subgraph C["Compuestos"]
direction TB
C1["Cadenas"]
C2["Arrays/Listas"]
C3["Objetos"]
C4["Tuplas"]
end

subgraph S["Sistemas de tipos"]
direction TB
S1["Tipado estático"]
S2["Tipado dinámico"]
S3["Tipado fuerte"]
S4["Tipado débil"]
end
```

### Operadores

```mermaid
flowchart LR

A["Operadores"]

A --> M
A --> C
A --> L
A --> S


subgraph M["Aritméticos"]
direction TB
P1["+"]
P2["-"]
P3["*"]
P4["/"]
P5["%"]
P6["**"]
end

subgraph C["Comparacion"]
direction TB
C1["=="]
C2["!="]
C3["<"]
C4[">"]
C5["<="]
C6[">="]
end

subgraph L["Lógicos"]
direction TB
L1["AND"]
L2["OR"]
L3["NOT"]
end

subgraph S["Asignación"]
direction TB
S1["="]
S2["+="]
S3["-="]
S4["*="]
S5["/="]
end

```

### Estructuras de control

```mermaid
flowchart LR

A["Estructuras de Control"]

A --> C
A --> B
A --> F

subgraph C["Condicionales"]
direction TB
C1["if"]
C2["else"]
C3["switch/match"]
end

subgraph B["Bucles"]
direction TB
B1["for"]
B2["while"]
B3["for-each/for-in"]
B4["do-while"]
end

subgraph F["Control de Flujo"]
direction TB
F1["break"]
F2["continue"]
F3["return"]
end
```

### Funciones 

Mecanismo principal de:

	+ Abstracción para dividir las tareas de un código
	+ Reutilización de la misma tarea en diferentes partes
	+ Hace solo una cosa muy puntual
	+ Nombre basado en la tarea que se quiere realizar
	+ Fácil de probar por su resultado esperado

#### Partes de una función:

	+ Declaración: Nombre, parámetros y cuerpo.
	+ Datos de entrada: por valor vs. por referencia, valores por defecto.
	+ Retorno de datos: valores de salida, múltiples retornos, void.
	+ Ambiente: ámbito de variables, closures, hoisting.


#### Algunas Tipo de funciones importantes

	+ Funciones anónimas: lambdas, arrow functions, callbacks.
	+ Recursividad: funciones que se llaman a sí mismas.
	
```mermaid
flowchart LR

A["Función"]

A --> C
A --> P
A --> T

subgraph C["Características"]
direction TB
C1["Abstracción"]
C2["Reutilización"]
C3["Especifica"]
C4["Nombre descriptivo"]
C5["Facil de testear"]
end

subgraph P["Partes"]
direction TB
P1["Declaración"]
P2["Parámetros"]
P3["Retorno"]
P4["Scope"]
end

subgraph T["Especiales"]
direction TB
T1["Anonimas"]
T2["Recursivas"]
end
``` 
	